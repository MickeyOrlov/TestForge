package io.testforge.example;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Minimal Spring Boot configuration for the example suite. TestForge modules
 * plug themselves in through auto-configuration — having them on the
 * classpath is enough, no annotations or imports required.
 */
@SpringBootApplication
public class ExampleApplication {
}
