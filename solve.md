## What problem does it solve?

Most QA automation projects start from scratch and slowly accumulate the same utilities:
polling waits, DB checks, Kafka probes, scoped mocks, test data generators,
contract checks and CI diagnostics.

TestForge provides these building blocks as a clonable JVM template.